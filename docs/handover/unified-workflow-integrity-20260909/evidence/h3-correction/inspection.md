# H3 correction evidence — 2026-09-10

This is one offline source-to-presentation chain using the exact input
`assets/h3/MiniMax_H3_AV_EncodeDecode_Inpaint.json` (SHA-256
`2dd64fe26c42281962e434841c458cc935b1d1858e83093b882bbaeb02dc3121`).

The configured Python 3.11 venv ran `port convert` with
`PYTHONHASHSEED=0`, `PYTHONPATH=.`, and `VIBECOMFY_ON_DEMAND_SCHEMAS=0`.
Conversion/build/compile/parity were reported successful. The edited Python
sets the authoritative prompt to `Edited H3 prompt: blue-hour dance with synchronized stereo audio.`, model to `edited-minimax-h3.safetensors`, seed to 123, steps to 31, duration to 7, LanPaint steps to 9, video to `edited-source.mp4`, keyframes to `{"0":"edited-start.png","42":"edited-mid.png"}`, and audio mask to `[{"start":1.25,"end":2.75}]`. Its SHA-256 is
`c803f26d928a61eb6bb21513d2f989ddff8b295abd90f8d4ec622c8d0170b7fb`.
Restricted load/build inspection retained those values on the corresponding runtime nodes.
The ordinary generated source has no `wf.connect` calls.

The same edited Python was then exported through `port export --to ui
--persist-sidecar`. Exact outputs:

| artifact | SHA-256 | exact check |
|---|---|---|
| exported UI JSON | `b398eee26a52518a638b0fd276fb3d7aeadb1a9b2b8f1e4d31260655df759653` | 20 nodes, 25 links; exact exported graph used for rendering |
| presentation sidecar | `719310b97aff19e578ceed88f84611969678ae271dadcdb53e55e3f78d86820e` | 20 non-empty `entries` |
| rendered inspection PNG | `8c0c23892d6502b0e6ef79cbdce9a1b50ee849a98344d4169adbced9b545e703` | 2200 × 1087; position-faithful overview plus native-resolution detail band, regenerated from the exact exported graph |

The source layout is absent, so fresh auto-placement is through the normal owner; the resulting evidence is readable and inspectable. The overview retains the authored cards, ports, and links without mutating their coordinates; when its wide graph must be fit to the overview canvas, lossy on-card labels are omitted and the full ordered node/port labels are rendered in the native-resolution detail band. All 20 exported node `properties.vibecomfy_uid` values are present and unique,
and the sidecar entry keys equal that UID set. The exported `RandomNoise`
node has `widgets_values[0] == 123`. The sidecar is fresh layout generated by
the normal export owner because the source fixture has no layout; it is not a
runtime/video-quality claim. The export reports one schema-less LanPaint node
and several low-confidence custom nodes, so strict-ready promotion, GPU
execution, and media-quality claims remain intentionally unmade.
