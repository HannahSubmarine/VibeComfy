# TEST-SIGNAL-20260911 — single planning ruling

Disposition: **adopt a focused test-signal amendment within the existing tasks.** Strengthen independent expectations and close the canonical workflow gaps; consolidate only demonstrated duplication as those owners change. No new testing framework, cleanup prerequisite, deletion quota, task family, review stage, or execution authorization. Preserve all existing tests until equivalent or stronger replacements are proven. Historical reviews, exhausted allowances and the S90 overrun remain unchanged. This is the one authorized adjudication, with no follow-up or extra review.

Evidence: read the [index and coordinator corrections](test-signal-audit-20260911.md) and all six reports: [browser](test-audit-20260911-browser-comfy-node.md), [editing](test-audit-20260911-editing-transactions-layout.md), [schema](test-audit-20260911-schema-registry-naming.md), [format](test-audit-20260911-workflow-format-roundtrip.md), [runtime](test-audit-20260911-runtime-agent-harness.md), and [infrastructure](test-audit-20260911-crosscutting-test-infrastructure.md). Governing documents are the [current direction](../../canonical-source-cleanliness-direction.md) and [current task dispatch map](../../tasklist.md). Source references below are relative to `/Users/hannahomalley/Documents/Codex/2026-09-08/goal-continue-the-existing-megado-plan/VibeComfy-integrity/.otto/worktrees/unified-workflow-integrity-20260909`, HEAD `c43e870d6c15ac9a31c0141d3212ae3a032d86ac`. Existing modified/untracked planning documents and corpus evidence were included; product source was clean at inspection. Only this ruling was written.

The 541 rows establish filename inventory coverage, not collected case coverage or exhaustive assertion review. Inspection depth varies. No collection, tests, timing, flakiness measurement, network acquisition, or new pass evidence occurred. Savings and current failure rates are unknown.

## Adopt, reject, defer

Adopt the reports' common conclusion: existing bundle/emitter, widget, edit/revision, independent readback and Comfy boundary suites are the right owners. Add missing v2 evidence there. Share small fixture builders and assertion helpers when their invariant is identical; keep explicit expected values outside the producer under test. Distinct entrypoints and failure conditions remain separate cases, including when parametrized.

| Supported candidate | Bounded disposition and replacement obligation |
| --- | --- |
| `tests/edgecases/test_determinism.py:30–62` | Consolidate the purported API-hash case: it actually hashes `result.text`, repeating text equality. Preserve separate-instance coverage and extend deterministic publication checks to both Python and JSON bytes, generation and custody identity. Rename any surviving hash claim accurately. No retirement before executable evidence. |
| `tests/test_wrapper_codegen.py:34–49` | Strengthen only the bare hexdigest truthiness assertion at line 41. Keep timestamp-independent source/hash and header checks. Assert a hand-specified provenance fingerprint and sensitivity to a changed provenance input under the existing hash contract. `porting/wrappers/codegen.py:45–68` hashes provenance input, not emitted file bytes; do not introduce an incorrect full-source equality assertion. |
| `tests/test_s2_named_field_emit.py:36–97` | Replace fixed source windows and name-presence alternatives with assertions on the intended call/field and independently expected node value/type/channel after reconstruction. Keep distinct node/range/unknown-field cases and lossless unresolved `widget_N` behavior. This file tests agent-edit emission: retain that boundary while adding canonical-emitter coverage. |
| `tests/test_h3_generated_editability.py:20–87`; `tests/test_emitted_no_helpers.py` | Preserve public-control and legacy compatibility assertions. Replace the new surface's embedded-custody/exact-syntax acceptance with whole-file source checks plus exact effective edges, slots, identity and v2 reload/edit assertions. Preserve helper role/provenance coverage. An old fixture passing does not certify freshly generated H3. |
| `tests/intent/test_edit_correctness.py:67–136` | Keep the fixture-verdict stub as plumbing/aggregation coverage. It cannot prove intent correctness because it returns the fixture's expected answer. For this workstream, prove selected edits against independently authored target/value/untouched-graph expectations and a wrong-target/wrong-value negative at the edit owner. Do not build another judge or require a live judge to certify deterministic graph fidelity. |
| Parity/golden overlap; apply/kernel/session repetition | Preserve corpus breadth, type/output checks, no-op, security, refusal and each real API door. Consolidate duplicated setup only when touched; replacing coarse counts or hashes requires exact semantic/identity assertions. Compile invariance, source snapshots and cross-language parity remain useful distinct protections. |

Reject the runtime report's empty-persistence-hole finding: `tests/test_live_agentic_harness_runner_persistence.py:1–3` explicitly preserves a D13 command path and imports the actual tests. Keep it; possible duplicate collection is optional future consumer/collection work, not a missing persistence contract. Reject treating installer sentinel cases as count-only: `tests/test_nodes_install.py:603–633` also proves result status, commit SHA and sentinel removal. Keep those conditions and live-owner refusal.

Reject blanket structural-edit refusal, forced v2 companions for unmarked legacy Python, mandatory persistence for previews, new locking machinery, and quarantine to shrink acceptance. Supported Python/typed edits retain their current behavior; unchanged-companion source edits obey the direction's direct construction compatibility. Only incompatible/unsupported changes refuse with regeneration guidance. Generation is publication identity, not a checksum demanding untouched Python bytes.

Defer broad lifecycle/payload/ownership regrouping, installer/cache consolidation, harness shims, research/demo/performance cleanup and suite-wide route reorganization. They stay unchanged in their existing lanes and required failures still count. These are optional future hygiene, not dependencies of canonical workflow delivery.

## Existing ownership and lanes

Extend `tests/README.md` and the existing task/acceptance records when the parent integrates this ruling; no parallel catalog or runner. Record the actual boundary crossed and mocked dependencies for each added case. Distinguish:

- Mocked transport/contracts: injected responses, transactions or adapters prove that specific contract only.
- Real boundary with fixture model: production conversion, loading, edit/apply, persistence or live canvas executes, with deterministic responses substituted only at the model/provider seam. A browser route stub proves browser behavior but bypasses that backend route.
- Live model/runtime: actual model/provider calls or GPU execution require their existing opt-ins. A real browser or subprocess alone is not live-model evidence.

`tests/test_canonical_user_paths.py:82–121` disables the Comfy converter and tests transaction persistence; preserve that coverage and add actual ingress cases. `tests/e2e/specs/agent_panel_turn.spec.mjs:177` intercepts agent-edit HTTP; retain it as browser coverage, but it cannot alone prove backend-to-pair persistence. T20 retained workflow/schema tests protect admission authority, not the new filesystem pair (`tests/test_t20_publication_custody.py:32–89`).

| Lane / existing mechanism | Ownership and acceptance role |
| --- | --- |
| Fast: `Makefile` `fast` / maintained `ci` | Keep current selectors and policy checks. Add only small canonical source/output/pair sentinels after their actual cost is measured during authorized execution. Fast feedback never substitutes for affected or full evidence. No CI/branch-protection change is implied. |
| Affected: existing Q0–Q6 selectors and direct pytest/Node selections | E0/T3: emitter, roundtrip, H3, widget/resolver, native scopes. T1: bundle, restricted/registry/copy loading and pair publication. T3: edit kernel/apply/recursive/revision. T2: canonical export/user paths and Comfy route. Include changed owners' consumers, not just the edited test file. `test_canonical_export_integrity.py` already exists: extend it and reconcile older “NEW module” wording. |
| Browser contracts: `browser-contracts`, complete `browser-smoke` | Keep canonical queue/delta/hash, lifecycle/malformed responses, graph projection, scope and dynamic-I/O suites. Python owns file-pair parsing/publication checks; browser tests consume real generated artifacts and assert consumer behavior. Do not duplicate the Python validator in JavaScript. |
| Browser acceptance: existing `tests/e2e` runner/specs/helpers | Extend actual canvas load/edit/apply/save/reload/export and refusal/no-change observations using fixture model responses through production edit/persistence owners. Retain dynamic socket, links, scope and stale-apply cases. Boot/overlay/count checks remain smoke, not fidelity proof. Missing browser observations remain missing evidence. |
| Full: configured offline suite, `broad-pytest` / `full-pytest`, complete browser smoke, applicable existing parity/snapshot/oracle/trust checks | Run required configured validation once on the integrated candidate, with bounded partitions if necessary and explicit collection coverage. Preserve coverage floor and existing required checks. Scheduled/manual broad CI is non-required in `.github/workflows/broad-python.yml`; that does not reduce this plan's final full-suite obligation. `make ci` alone is insufficient. |
| Live/provider/GPU: existing live, RunPod and Comfy opt-ins | Retain existing coverage and policy; do not move it into fast/offline validation or silently discard it. No new live run, provisioning or judge requirement follows from cleanup. Report it separately from fixture-backed evidence. |

Execution preflight must inspect commands rather than assume target names are offline-safe: `Makefile` `oracle` sets `VIBECOMFY_COMFY_SMOKE=1`, while e2e/trust preflight targets may install dependencies. Reuse the configured available environment/runner without new installs or opt-ins; an unavailable required boundary is an evidence gap, not permission to skip or claim PASS. Keep Python 3.11+, `PYTHONHASHSEED=0`, existing plugins, 300/600/900-second limits and the 1 GiB output ceiling. `pyproject.toml:104` excludes GPU only, not every network path. Deliberately exercise production-on schema acquisition with local pinned resolver fixtures, then prove zero post-freeze acquisition. Keep failure-then-success context checks inside one test because `tests/conftest.py:203` resets context between tests.

## Retirement proof and minimal assertions

Before retiring or weakening any existing assertion, record in its existing task evidence: **old nodeid/invariant → replacement nodeid/assertion → independent oracle → demonstrated defect detection and diagnostic**. Retain each unique positive, refusal and failure condition, caller boundary and corpus case. Run the replacement on the good candidate and a minimized known defect or deliberate local perturbation; it must fail for the intended reason and identify the relevant scope/node/field/slot or publication stage. Restore perturbations and pass affected checks. Preserve existing passing coverage until that evidence exists. Renaming/moving tests also requires updating actual selectors, fixtures and quarantine consumers using the existing stale-entry audit. No static candidate in this ruling is a safe deletion yet.

Use a small test helper next to the existing emitter tests to traverse the entire generated AST: module declarations, metadata, nested definitions, constructor/handle expressions and finalization. Validate supported emitted forms and prohibited data roles, not only names. Check readable topological construction; no hidden custody/raw graph payload, loader/replay code, raw ID wiring, duplicate value authority or giant custody finalizer; exactly the intended Python and companion, without an executable wrapper. Test renamed module custody, nested replay, metadata overwrite and disguised finalizer payload mutations. Keep positive controls for legitimate JSON-valued runtime arguments, shared constants, long literals, aliases, output unpacking and supported nested definitions. Unknown emitted syntax fails acceptance explicitly; this is not a universal Python analyzer or a new restriction on legacy/user-authored code. Use existing emitter wrapping conventions, not line-count/byte quotas or a new formatter.

Pair source checks with independently authored expected values/types, effective edge endpoints and multiplicities, slots, public inputs and explicit zero/single/ordered outputs (distinct from node output arity). Compare IDs/UIDs, scoped bindings, native ports, helper provenance, model strings/requirements and presentation separately. API/GraphBuilder agreement or matching digests alone can share defects. In particular, `tests/parity/test_independent_readback.py:43–110` avoids UI normalization but still shares schema-name resolution and UI-only classification; retain it and supplement with hand-authored position/identity/role expectations. Preserve wired PreviewAny and runtime-consumed text according to the governing role contract.

Extend `test_workflow_bundle.py` with the closed v2 parser and pre-constructor validation cases: duplicate/unknown keys, malformed/non-finite fields, scope-local duplicate or missing bindings, native-port witnesses, invalid presentation references, missing/malformed/swapped companions, same-custody/different-generation mismatch, and explicit staged companion selection. JSON cannot restore missing edges, instantiate helpers or overwrite constructor values. Test values/comments/formatting with unchanged JSON, presentation-only revision changes, stale approvals, no-op behavior, and failed-build recovery with an enclosing context preserved. Use one fixture per invariant plus selected interactions, not the full Cartesian product.

Reuse `_atomic_publish_pair` and its existing injection tests (`vibecomfy/workflow_bundle.py:1834`; `tests/test_workflow_bundle.py:1072,1112`). Validate before replacement; failed validation leaves destinations unchanged; ordinary publication failure restores the previous pair or leaves no newly created partial pair. At an injected read between replacements, require mixed-generation refusal, followed by a valid pair after successful completion/rollback. Do not promise simultaneous filesystem replacement or unchanged visible bytes during that interval. Preserve explicit rollback/cleanup-failure reporting; do not claim successful restoration when recovery itself fails. No concurrent-writer subsystem is added.

## Validation loop and incorporation

| Existing task / route | Concrete planning addition, dependencies and proof |
| --- | --- |
| E0/T3 — normal | Establish small baselines before relevant changes: source layout, exact outputs, independent semantic/identity expectations, Python/typed parity, same-source single admission/report identity, provider freezing and context recovery. Trace corpus 01/16/17/18 before classifying outcomes. Record old-to-replacement mappings only for touched tests. Fixture preparation can overlap independent work. |
| T1 and E1/T1 — existing routes | Add pair/parser/load/publication assertions alongside their existing implementation seams; serialize shared files. E1's original coupled hard kernel retains its existing assignment only if needed; cleanup does not rerun or expand it. Each local change receives its focused tests. |
| T3 with T1/T2 — normal | Integrate value/channel/model/audio regressions, scoped identity, annotations, supported edits and refusal/rollback evidence. Preserve exact Case 07 label titles and Case 08 note bodies, plus empty/Unicode/whitespace and definition/instance attachment cases. These consume the relevant bundle/emitter/resolver changes. |
| T2 — normal | Compare the same supported draft and malformed schema/boundary through actual CLI, public SDK and canvas ingress, plus CLI/bundle UI export under matching authority. Keep optional legacy layout, recovery and force-drop contracts. Characterize schema-less and schematized unknown output names; fix only reproduced public drift and forbid silent slot-zero rewiring. |
| E2/T4 — normal | After required fixes/focused evidence, regenerate and inspect exact H3 and all original corpus outcomes, complete lifecycle/browser evidence, reconcile docs/help/selection wording, then run final configured validation. Optional test hygiene does not block these dependencies. |

The executable loop, once separately authorized, is: reproduce the expected failure at the owning boundary; minimize its fixture while retaining the failing source fact; fix that owner; rerun the regression and affected consumers; then replay the original input and integrated artifacts. Store concise expected-versus-actual diagnostics and exact source/candidate/environment identities in existing evidence. A reduced fixture never replaces its original corpus check.

Use all original inputs and raw receipts in `docs/handover/unified-workflow-integrity-20260909/evidence/workflow-conversion-corpus-20260911-20/`. Expectations follow its adjudication, not its superseded initial triage: preserve positives 03–09; fix 12's absent/switch channel values, 19's authored model strings with separately proven arity, and 20's `IAMCCS_MultiSwitch:0 → VHS_VideoCombine.audio` edge. Trace 01/16/17/18 against exact source membership and pinned evidence; refuse invalid/ambiguous source or fix importer loss. Preserve intentional unsupported-boundary refusals 02/11/13/14/15 and malformed-shape 10; add a separate valid depth-two/repeated-instance positive. All 20 must match justified expectations with zero unexplained discrepancies, not necessarily twenty conversions. Every positive materializes a validated deterministic v2 pair; every refusal has the expected contextual diagnostic, no partial output and unchanged prior destinations. Preserve the seven historical positive artifacts as baseline evidence until v2 replacements are proven.

For H3, use the exact saved input `docs/handover/unified-workflow-integrity-20260909/assets/h3/MiniMax_H3_AV_EncodeDecode_Inpaint.json`, recorded SHA-256 `2dd64fe26c42281962e434841c458cc935b1d1858e83093b882bbaeb02dc3121`. Generate through the normal shared path and retain the actual generated `.py`, `.vibe.json`, edited pair, exported UI graph, hashes and command receipt under existing E2 evidence ownership. The committed builder, test fixture and historical `evidence/h3-correction/` files are comparison artifacts, not fresh v2 proof.

Edit prompt, model, prepared source, video/audio masks, duration, seed and audio interval through the supported Python and Comfy Node paths. Observe apply, save, reload and export; compare exact intended changes and untouched wiring/identity/native ports, requirements/readiness, presentation and revisions. Verify unchanged-companion compatible source edits, deterministic regeneration for unchanged publication inputs, stale-approval refusal and injected rollback. Do not demand edited and original publications have identical bytes. Inspect the whole generated Python and render the graph from the exact exported artifact, including supported subgraph/boundary details; retain the view/screenshot and criterion-linked findings. Missing models remain valid persistence drafts where supported, without implying GPU execution readiness or video quality.

Final acceptance retains I1–I10, all required positives/refusals, source/graph inspection, configured offline and browser coverage. Report skips, deselections, expected failures and quarantine reasons separately; neither old PASS receipts nor README baseline-failure claims certify this candidate. No new runtime estimate is justified by static inspection; retain the direction's 4–7 focused engineer-days plus 1–2 contingency, with optional hygiene excluded. Parent next action: integrate this ruling into the existing planning artifacts only. Implementation remains NOT RUN; this ruling requests no further adjudication.
