> Current authority (2026-09-11): **delivery approved** for the source-cleanliness
> amendment. Execute the plan and required validation from the active
> EXECUTION-READY.md entrypoint. Older planning-only statements below are
> historical and do not override this authorization.

# Unified workflow integrity handover

Current delivery launch: [EXECUTION-READY.md](EXECUTION-READY.md).
Use that entrypoint and its current status/configuration; older launch/counter
prose below is historical. Preparation is complete as documentation, not a
claim of installed browser dependencies, new review allowance or test PASS; those
require delivery evidence.

Test-signal planning supplement: [focused cleanup plan](test-signal-cleanup-plan.md)
and [six-slice Luna inventory](evidence/test-signal-audit-20260911/test-signal-audit-20260911.md).
This is preparation only; no test deletion, lane suppression or new acceptance PASS.

This is the active Megado handover for VibeComfy's canonical workflow integrity and editable Python work. It consolidates the save/export integrity plan with the absorbed Pythonic-emission contract. Delivery implementation and validation are in progress in the isolated candidate checkout; the exact evidence is recorded under the active run. Final review PASS, merge, deployment, and generated-video success remain distinct claims.

Project: `VibeComfy`

Remote: `https://github.com/peteromallet/VibeComfy.git`

Base: `20975e25f0401b64f51dfeef5d1250cfbd97600f`

Receiving branch: `main`

Receiving mode: `delivery`

The handover preserves D1, I1–I10, E0–E2, C0/T0–T4, review counters, and the two existing review stages. Source closure is resolved and pinned dependency reads are verified: no dirty source patch is required. Verify your checkout and available environment/capabilities as described in provenance.md and dependencies.md.

## Start

```sh
git clone --branch main --single-branch https://github.com/peteromallet/VibeComfy.git VibeComfy-unified-workflow
git -C VibeComfy-unified-workflow rev-parse HEAD
```

Read `goal.md`, `northstar.md`, `run.yaml`, `status.md`, `consolidation.md`, `plan.md`, `tasklist.md`, `acceptance-ledger.md`, and `execution-start.md`. The H3 source input is `assets/h3/MiniMax_H3_AV_EncodeDecode_Inpaint.json`; its SHA-256 is recorded in `evidence/h3-main-baseline.md`.

Delivery is now authorized. Preserve roles, counters, stages, caps, and boundaries while executing the plan.

The ready-to-send message is [assets/handover-message.md](assets/handover-message.md). The user authorized publishing these plan documents directly to main; the receiver is authorized to execute the plan and required validation immediately.

Latest requested [Astra end-state sense check](evidence/astra-end-state-sense-check.md): two evidence clarifications are adopted in the acceptance ledger and task briefs. No product tests or implementation are claimed.

**Execute now:** the latest user authorization is delivery, superseding older planning-only statements. Follow run.yaml and the existing plan through implementation and validation.
